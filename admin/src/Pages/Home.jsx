import React from "react"
import AllProducts from "../Components/AllProducts";

const Main = (props) => {
    return (
        <div>
            <AllProducts />
        </div>
    )
};

export default Main;
